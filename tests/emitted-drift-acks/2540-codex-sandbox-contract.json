{
  "version": 1,
  "paths": {
    "new-milestone.md": "#2540 — workflow guidance distinguishing a sandbox violation from a missing agent, which this workflow had no wording for before. Size growth only; the emitted agents/*.toml deltas are attributed by rule since #2757.",
    "new-project.md": "#2540 — same change: the sandbox-violation vs missing-agent distinction surfaced here. Size growth only.",
    "agents/gsd-nyquist-auditor.agent.md": "#2540 review round 4 — the Copilot converter now parses the tools: contract through the agent-tools-contract seam, so this agent's emitted tools line goes from ['- read'] (six tools silently dropped) to ['read', 'edit', 'execute', 'search', 'skill']. The delta is the intended fix. It is unattributable only because the copilot-agent-rename rule declares no `transforms`, unlike agents-toml-derived which carries AGENT_TRANSFORM_SRCS since #2757 — and src/runtime-artifact-conversion.cts, the file that changed, is already IN that list. Narrow provenance-table gap raised on the PR rather than patched here, since the table belongs to epic #2719; this is the unblock path its own docblock prescribes.",
    "agents/gsd-security-auditor.agent.md": "#2540 review round 4 — same converter fix, same table gap. This agent's contract is the repo's other block list (Read/Bash/Glob/Grep/Skill), so its emitted Copilot tools line moves from ['- read'] to ['read', 'execute', 'search', 'skill']. These two are the only block-list agents in the repo, which is why exactly two emitted paths move."
  }
}
