{
  "gate": "api-coverage.verify-pre",
  "sourceIssue": "#2366",
  "fixtures": [
    {
      "file": "multi-table-with-summary.md",
      "role": "coverage-matrix",
      "pairedPlan": "Integrate the Stripe API for payment processing.",
      "expectedCounts": { "surface": 3, "integrate": 1, "optout": 2 },
      "expectedErrorCount": 0,
      "expectedBlock": false,
      "note": "Self-contained 15-line COVERAGE.md from #2366's own reproduction: one canonical matrix (search/skip), a second section-split canonical table (widget) for a transferred capability, and a decoy 3-column summary table whose 2nd cell reads 'INTEGRATE'. Today's parser invents a 'tier' capability from the summary table's header row (silent corruption, zero errors) and drops 'skip' (bolded decision rejected), while also spuriously erroring on the second header. Expected: exactly 3 rows (search, skip, widget), 0 errors."
    }
  ]
}
